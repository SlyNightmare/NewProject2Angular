export class Playlist {
    id: number;
    name: string;
    account: Account;
    tracks: list;

    constructor(id:number, name:string, account:Account, tracks:list) {
        this.id,
        this.name,
        this.account,
        this.tracks
    }
}